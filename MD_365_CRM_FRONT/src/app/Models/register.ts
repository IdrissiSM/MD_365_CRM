export interface Register {
  firstname: string,
  lastname: string,
  username: string,
  email: string, // the one we already have
  password: string,
  jobtitle: string,
  gendercode: number,
  statecode: number, // always true
}